const add = (a, b) => {
  return a + b;
};

const sub = (a, b) => {
  return a - b;
};

const name = "Rahul Shinde";

module.exports = { add, sub, name };

// We can also add new name like a, b, and c for functions
// module.exports = { a: add, b: sub, c: name };
